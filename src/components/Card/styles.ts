import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  card: {
    flexDirection: 'row',
    borderWidth: 2,
    borderColor: '#efefef',
    padding: 10,
    borderRadius: 5,
    backgroundColor: '#fff',
  },
});

export default styles;
